* Program to shutdown the ZOOM client or SERVER
HIDE POPUP ALL
CLOSE ALL
IF MODE="S"
 DO SVR_DOWN
ELSE
 USE ZOOM.CFG
 STORE DOBACKUP TO _BACKUP
 CLOSE ALL
 IF _BACKUP=.T.
  DO BACKUP
 ENDIF
ENDIF
QUIT
